# Tích hợp LuxCare API

Backend được triển khai từ repository LuxCare riêng. Đặt origin của backend trong `mobile/.env`; không có tài khoản hoặc secrets mẫu trong repository mobile.

Đợt 68 POST `/api/v1/payroll/period-input-variables/:id/activate` hoặc `/retire`, quyền quản lý kỳ. Backend lọc company/status: draft hoặc retired sang active, active sang retired; ghi activatedBy/retiredBy và bỏ retiredBy khi áp dụng lại, không tăng version/không nhận expectedVersion. Mobile dùng service FE, đối chiếu response ID/company/status cùng code/name/unit/default/description/version đã xem; thay đổi đồng thời có thể đã ghi trạng thái nhưng response bị từ chối do nội dung khác, nên phải tải lại, không retry. Sau thao tác tải lại cả inputs để cập nhật danh sách active. Service backend không tự đánh dấu needsInputRefresh hoặc tính lại kỳ trong hai thao tác này.

Đợt 67 dùng GET/POST `/api/v1/payroll/period-input-variables`, quyền đọc/quản lý kỳ tương ứng, tái sử dụng service FE. Danh mục scope company (không phải branch); backend tạo status draft/version 0, unique company/code và chưa đưa biến vào danh sách active của inputs. Mobile gửi code/name/unit/defaultValue/description, bỏ mặc định trống, giữ 0; giới hạn mã 64/tên 200/mô tả 1000, mặc định không âm và percent 0–100. Response được kiểm tra company/draft/version/nội dung, lỗi kể cả duplicate key yêu cầu tải lại, không retry. Chưa kích hoạt hoặc tự tính lại lương.

Đợt 66 tạo đối soát qua cùng PUT inputs với expectedVersion 0 (backend upsert). Mobile lấy roster theo company/branch, chỉ chọn người cùng chi nhánh và chưa có item trong phản hồi inputs đã tải, dùng lại form/validator với version gốc 0. Backend unique company/branch/period/employee và version filter xử lý cạnh tranh; duplicate key có thể trả lỗi chung, app không tự chuyển sang cập nhật hoặc retry. Kiểm tra version 1 và các giá trị phản hồi, tải lại để đối soát cả khi lỗi sau ghi/audit. Roster vẫn áp dụng quyền API hiện có; lỗi tải không biến thành danh sách rỗng.

Đợt 65 sửa customValues qua PUT đối soát hiện có. Backend $set customValues thay map, vì vậy mobile hợp nhất giá trị mới với map đã tải và giữ expectedVersion để phát hiện cạnh tranh. Chỉ sửa mã trong danh mục active, mã an toàn theo mẫu chữ/số/gạch dưới; phần trăm mới giới hạn 100 ở mobile. Hoàn tác biến active đã lưu gửi clearFields custom.CODE, không gửi customValues. Mobile chặn sửa map và gỡ nhánh custom đồng thời để tránh MongoDB path conflict giữa $set customValues và $unset customValues.CODE. Phản hồi được kiểm tra cả biến mới/cũ/gỡ, lỗi không retry; chưa thay backend hoặc thử API thật.

Đợt 64 bổ sung clearFields vào PUT đối soát hiện có. Mobile chỉ cho chọn sáu trường cơ bản đang có giá trị, loại trùng và bỏ giá trị thay thế của trường được gỡ khỏi payload. Backend buildPeriodInputChanges dùng $unset để bỏ trường; không ghi 0/null. Lý do/expectedVersion/quyền và xử lý lỗi giữ nguyên. App kiểm tra trường vắng trong phản hồi, giữ các trường khác và customValues; tải lại để đối soát, không tự tính lại lương. Chưa gửi đường dẫn custom.* từ mobile.

Đợt 63 dùng PUT `/api/v1/payroll/periods/:periodKey/inputs/:employeeId`, quyền `payroll-period:manage`, tái sử dụng savePeriodInput với ID đã mã hóa. Mobile chỉ sửa bản ghi đã tải, gửi sáu trường cơ bản thực sự đổi kèm reason/expectedVersion; không gửi customValues/clearFields hoặc phạm vi từ form. Backend kiểm tra kỳ regular còn draft/chưa có kỳ, so phiên bản, tăng version, đặt needsInputRefresh và ghi audit. Các bước lưu/cập nhật cờ/audit không bọc transaction trong service hiện tại: lỗi có thể đến sau khi dữ liệu đã ghi; mobile không retry và yêu cầu tải lại. Lưu không tự tính lại lương. Chưa UAT cạnh tranh giữa chốt kỳ và sửa đối soát.

Đợt 62 tra cứu GET `/api/v1/payroll/periods/:periodKey/inputs`, quyền `payroll-period:read`; mobile thêm phân hệ HR/company/chi nhánh. Tận dụng getPeriodInputs, mã hóa periodKey; response data gồm items, variables active, editable và needsRefresh. Các bản ghi là giá trị đối soát đã lưu, không phải roster hay kết quả lương đã tính. App kiểm tra đúng kỳ, nhân viên không trùng, số hữu hạn không âm, phiên bản và cấu trúc biến; mặc định biến hiển thị riêng, mã cũ vẫn được giữ. Cờ editable theo kỳ regular mới nhất (không có kỳ hoặc draft); needsRefresh phản ánh needsInputRefresh. Chưa gọi save/bulk hoặc tự tính lại, chưa phân trang server; mobile chỉ giới hạn số bản ghi render mỗi lượt.

Đợt 61 bổ sung metadata vào các form thanh toán: create/confirm gửi paymentDate tùy chọn; create/confirm/cancel/reverse gửi evidenceUrl/note tùy chọn theo schema hiện có. Ngày YYYY-MM-DD được xác minh rồi chuyển 00:00 UTC+7 sang ISO UTC. URL HTTP/HTTPS tối đa 2000 ký tự, note trim tối đa 1000; ô trống được bỏ khỏi payload (backend dùng giá trị truthy nên không xóa metadata cũ). confirmPayment/cancelPayment/reversePayment nhận tham số payload tùy chọn, lời gọi FE cũ vẫn giữ body rỗng. App xem lại và đối chiếu trường đã gửi trong phản hồi; cancel/reverse không gửi ngày vì backend không cập nhật ngày ở hai thao tác này. Ghi chú mới thay ghi chú hiện tại, không phải một trường lý do audit riêng. Chưa upload/chỉnh metadata độc lập sau giao dịch.

Hủy/đảo dùng POST `/api/v1/payroll/payments/:id/cancel` và `/reverse`, quyền `payroll-payment:manage`; mobile thêm quyền đọc kỳ/thanh toán và chi nhánh. Cancel chỉ draft, không giới hạn trạng thái kỳ, không cần phân bổ; reverse chỉ confirmed và kỳ closed/paid (mobile yêu cầu phân bổ hợp lệ). Service giữ chữ ký FE, mã hóa ID, không gửi body để giữ metadata cũ. Backend chuyển cancelled/reversed trong transaction và audit; cancel giữ quyết toán, reverse loại phân bổ khỏi tổng confirmed và tính lại trạng thái kỳ. App kiểm tra phản hồi đúng khoản/kỳ/trạng thái/tổng/phân bổ, không retry và tải lại cả kỳ. Không gọi hoàn tiền ngân hàng; không tự thu hồi hay phát hành lại phiếu lương.

Xác nhận thanh toán gọi POST `/api/v1/payroll/payments/:id/confirm`, quyền `payroll-payment:manage`; mobile tận dụng confirmPayment, không gửi body để giữ ngày/chứng từ/ghi chú khoản nháp. Backend chỉ nhận draft thuộc kỳ closed, đối soát phân bổ và số dư trong transaction, chuyển confirmed, dùng ngày nháp hoặc thời điểm hiện tại, cập nhật trạng thái kỳ và audit. Mobile yêu cầu thêm quyền đọc kỳ/thanh toán và chi nhánh; đối chiếu data đúng khoản/kỳ/số tiền/từng phân bổ rồi yêu cầu tải lại cả kỳ (runStatus nằm ngoài data, service không trả trường này). Lỗi hoặc timeout không tự retry. Đây là ghi nhận chi trả, không phải lệnh chuyển tiền ngân hàng.

Tạo thanh toán nháp gọi POST `/api/v1/payroll/runs/:id/payments`, quyền `payroll-payment:manage`, body amount/lines/idempotencyKey và note tùy chọn. Mobile yêu cầu thêm đọc kỳ/thanh toán và chi nhánh, chỉ closed; dùng employeeId từ effectiveLines, cộng amount từ phân bổ dương nguyên VND, ghi chú tối đa 1000 ký tự. Backend kiểm tra settlement theo khoản confirmed, tạo draft và audit; replay key có thể trả bản ghi đã có. App chỉ xác nhận response draft khớp run/tổng/toàn bộ phân bổ, không tự retry hay confirm. Chưa gửi paymentDate/evidenceUrl/correlationId từ form mobile; draft không tự chuyển tiền.

Mở lại dùng POST `/api/v1/payroll/runs/:id/reopen`, quyền `payroll-period:manage`, body expectedVersion/reason (trim, 1–1000 ký tự). Mobile yêu cầu thêm quyền đọc/phạm vi chi nhánh, chỉ review/closed; backend chặn paid và confirmed payments, chuyển draft/version +1, xóa closedBy/closedAt/effectiveSnapshot và audit lý do. App kiểm tra run trực tiếp, không tự retry hoặc đảo thanh toán để vượt điều kiện. Phiếu cá nhân được lọc khỏi danh sách trong kỳ draft; không xóa các tệp đã chia sẻ hoặc tự phát hành lại.

Chốt kỳ gọi POST `/api/v1/payroll/runs/:id/close`, quyền `payroll-period:manage`, body expectedVersion; mobile yêu cầu đọc kỳ/phạm vi chi nhánh và review/version hợp lệ. Backend xác minh active revision completed, checksum và effective snapshot đã pin (có đường tương thích cho kỳ review cũ), chuyển closed/version +1, ghi closedBy/closedAt và audit. Mobile tận dụng closeRun/ReviewPayrollRun, kiểm tra phản hồi run trực tiếp, không gửi lại sau lỗi hoặc thay số liệu để bỏ qua kiểm tra. Xuất báo cáo/phát hành phiếu là thao tác riêng sau tải lại, không tự thanh toán.

Duyệt kỳ dùng POST `/api/v1/payroll/runs/:id/review`, quyền `payroll-period:manage`, body expectedVersion. Mobile tận dụng reviewRun của FE, yêu cầu thêm quyền đọc/phạm vi chi nhánh và draft/version hợp lệ. Workflow backend kiểm tra version, trạng thái, issues blocking và pin effective snapshot khi review; tăng version, lưu reviewedBy và audit. Response data là run trực tiếp; mobile đối chiếu _id/periodKey/status review/version +1, không tự retry hoặc chốt kỳ. Nhãn review hiện hiển thị “Đang kiểm tra”.

Tính lương dùng POST `/api/v1/payroll/runs/:id/calculate` với quyền `payroll-period:manage`, expectedVersion và Idempotency-Key UUID. Mobile thêm calculateOperationalRun, giữ calculateRun cũ của FE; chỉ mở thao tác cho draft có version hợp lệ trong scope phiên. Backend lấy PayrollAttendanceSnapshot, tạo revision, kích hoạt revision với version +1, lưu idempotency result, audit và chiếu effectiveLines. Thiếu bản công hoặc lỗi đầu vào có thể được bọc thành PAYROLL_CALCULATION_FAILED; lỗi được hiển thị và yêu cầu tải lại, không tự gửi lại. Response completed không xác nhận đủ điều kiện duyệt; app không dùng totals nguồn thay cho số liệu có hiệu lực. Timeout JSON hiện theo cấu hình client; chưa UAT thời gian tính kỳ lớn.

Khóa công dùng POST `/api/v1/payroll/runs/:id/lock-attendance`, quyền `payroll-period:manage`, body expectedVersion. Mobile chỉ mở sau sync thành công không có blocking và gửi version trước sync +1 đã đối chiếu phản hồi. Backend yêu cầu draft/đúng version/không blocking và sync job succeeded có result.runVersion trùng expectedVersion, tạo PayrollAttendanceSnapshot từ job.employees, tăng version và audit lock_attendance. Response `{run,snapshot}` được đối chiếu trước báo thành công. Snapshot có unique companyCode/runId; mobile chưa có thao tác thay thế/mở khóa, không tự retry hoặc bỏ qua lỗi khóa trùng. Không gọi endpoint legacy khóa kết quả công.

Đồng bộ công dùng POST `/api/v1/payroll/runs/:id/sync-attendance`, quyền `payroll-period:manage`, body expectedVersion và header Idempotency-Key UUID. Backend yêu cầu draft/đúng version, đọc AttendancePeriodResult theo kỳ và scope, tạo job, cập nhật run.issues/version +1, lưu employeeCount/blockingIssueCount trong job.result và audit sync_attendance. Mobile đối chiếu job.runId/operation/idempotencyKey/payload.expectedVersion/status cùng runVersion và số lượng; không tự gửi lại sau lỗi. Cùng key/payload có thể được backend replay, nhưng mobile hiện yêu cầu tải lại trước thao tác mới. Endpoint không tạo snapshot khóa công hay tính lương; kết quả công cần được tổng hợp trước trên web.

Tạo kỳ nháp dùng POST `/api/v1/payroll/runs`, quyền `payroll-period:manage`, body periodKey/startDate/endDate/type=regular. Mobile yêu cầu thêm quyền đọc để xác nhận 404 PAYROLL_RUN_NOT_FOUND trước khi hiển thị nút tạo, phạm vi Bearer/x-branch-id; không gửi tenant hoặc branch trong body. Backend tạo draft/version 0/lines rỗng, audit create_run và chặn kỳ regular chồng khoảng ngày (409 PAYROLL_PERIOD_OVERLAP). Mobile tạo theo tháng lịch, không tự retry khi lỗi; tải lại kỳ để đối soát. createOperationalRun được thêm vào service chung, giữ createRun legacy POST periods/:periodKey/run vì endpoint legacy thực hiện tính lương từ công đã khóa. Chưa giao diện tạo supplemental hoặc khoảng ngày tùy chỉnh.

Lỗi kỳ lương dùng GET `/api/v1/payroll/runs/:id/issues` với quyền `payroll-period:read`, scope công ty/chi nhánh. Backend trả `run.issues || []`, không phân trang hay tính lại lỗi; không có run trả 404 PAYROLL_RUN_NOT_FOUND. Mobile thêm type payrollIssue và getRunIssues trong service FE dùng chung, kiểm tra code/message/severity cùng trường tùy chọn, đối chiếu runId nếu có vì một số nguồn chỉ có employeeId. Mức độ ngoài blocking/warning được giữ nguyên. Danh sách trống không xác nhận kỳ đủ điều kiện duyệt/chốt. Backend hiện không có route sửa/xóa adjustment.

Thu hồi phiếu gọi POST `/api/v1/payroll/runs/:id/payslips/:employeeId/withdraw` với `payroll-payment:manage`, không có body/lý do/expectedVersion. Backend cập nhật có điều kiện publication published trong scope công ty/chi nhánh sang withdrawn và lưu withdrawnBy/withdrawnAt; không tìm thấy trả 404 PAYSLIP_NOT_FOUND. Mobile chỉ cung cấp thao tác cho phiếu hiện có trong publishedEmployeeIds và effectiveLines của kỳ closed/paid, kiểm tra phản hồi đúng phiếu, không tự retry. API không xóa tệp đã tải hoặc bản sao trong ứng dụng nhận; cần UAT quyền truy cập sau thu hồi.

Phát hành phiếu gọi POST `/api/v1/payroll/runs/:id/payslips/publish`, quyền `payroll-payment:manage`, body employeeIds tường minh. Mobile yêu cầu thêm đọc kỳ để tra cứu; chỉ chọn nhân viên trong effectiveLines của kỳ closed/paid. Backend lọc mã hợp lệ, upsert từng publication với checksum hiện hành, trả mảng docs published; Promise.all không đảm bảo rollback toàn bộ khi lỗi. App kiểm tra đúng runId/employeeId/status và đủ danh sách, chặn gửi lại trong lần xác nhận, yêu cầu tải lại sau lỗi. publishedEmployeeIds từ GET kỳ chỉ gồm publication khớp checksum có hiệu lực. Phát hành không gửi email hoặc push từ mobile.

Xuất Excel gọi POST `/api/v1/payroll/runs/:id/exports`, body `{type}` với detailed/insurance/pit/bank_transfer. Route yêu cầu `payroll-payment:read`; bank_transfer cần thêm `payroll-payment:manage`. Backend kiểm tra scope công ty/chi nhánh và kỳ closed/paid (409 PAYROLL_RUN_NOT_CLOSED), dựng workbook từ effective lines/checksum và tạo PayrollExportJob completed trước khi trả byte XLSX. Mobile không tự retry; tải lại có thể tạo thêm export job, không tạo giao dịch chuyển tiền. Service chung giữ exportWorkbook trả Blob cho web, bổ sung downloadWorkbook trả Uint8Array cho native qua transport Bearer/x-branch-id. Native giới hạn 20 MB, timeout 120 giây và giữ tệp đã chia sẻ trong cache.

Nhật ký kỳ gọi GET `/api/v1/payroll/periods/:periodKey/audit` với `payroll-period:read`, phạm vi công ty/chi nhánh xác thực. API trả mảng mới nhất trước, không phân trang hoặc bổ sung tên actor. Mobile hiển thị actorId, chỉ đọc reason/adjustmentId từ metadata; không coi nhật ký là cam kết mọi thao tác đều đã được backend ghi nhận.

Tạo điều chỉnh POST `/api/v1/payroll/periods/:periodKey/adjustments` với quyền quản lý kỳ, body employeeId/kind/amount/reason, trả bản ghi pending. Mobile dùng roster `/api/v1/auth/users` theo công ty/chi nhánh để chọn nhân viên, cần quyền đọc endpoint đó; không tự mở quyền khi tải lỗi. API tạo chưa có idempotency, mobile chặn gửi lại sau lỗi cho đến khi đóng/tải lại. Form mobile giới hạn tiền nguyên VND không âm và lý do tối đa 2000 ký tự.

Duyệt/từ chối điều chỉnh dùng POST `/api/v1/payroll/periods/:periodKey/adjustments/:id/approve` hoặc `/reject`, quyền `payroll-period:manage`. Backend cập nhật có điều kiện `status: pending`, trả 409 nếu đã xử lý và tăng version; không nhận expectedVersion hay lý do từ chối riêng. Cả hai route có thể tính lại kỳ draft legacy, lỗi tính lại được ghi log; phản hồi thành công xác nhận trạng thái điều chỉnh, không đảm bảo đã tính lại lương. Mobile tải lại cả kỳ và danh sách khi đóng xác nhận, không tự retry mutation.

Điều chỉnh lương dùng GET `/api/v1/payroll/periods/:periodKey/adjustments`, quyền `payroll-period:read`, scope công ty/chi nhánh xác thực và kỳ. API trả tên nhân viên cùng mảng điều chỉnh, chưa phân trang. Trạng thái approved và snapshotted được hiển thị riêng; mobile không tự cộng vào thực nhận. Luồng tra cứu không yêu cầu kỳ đã tồn tại bảng lương.

Lịch sử thanh toán gọi GET `/api/v1/payroll/runs/:id/payments` với quyền `payroll-payment:read`. API trả mảng sắp theo ngày tạo giảm dần trong phạm vi công ty/chi nhánh xác thực; mobile kiểm tra runId, lọc trạng thái tại máy và chỉ cộng amount của bản ghi confirmed. Dòng phân bổ dùng employeeId đối chiếu tên trong bảng lương, giữ mã nếu không tìm thấy tên. Chưa có API phân trang cho luồng này.

Tra cứu kỳ dùng GET `/api/v1/payroll/periods/:periodKey/run` với `payroll-period:read`. Mobile chỉ dùng `effectiveLines`; `effectiveError` được hiển thị thành lỗi thay vì dùng `lines` gốc. Backend hiện có thể tự sửa snapshot review khi checksum lệch trong quá trình GET; mobile không gửi yêu cầu tính/duyệt/chốt từ màn hình tra cứu. Chi tiết dòng vẫn yêu cầu quyền đọc/quản lý thanh toán hoặc quyền sở hữu phiếu đã phát hành ở backend.

Tải phiếu lương dùng GET `/api/v1/payroll/runs/:id/payslips/:employeeId/print` với Bearer token. Response hiện là `text/html`, có mẫu in và `window.print()` của backend; mobile lưu nguyên byte vào `.html` và mở bảng chia sẻ, không thực thi HTML trong app. API kiểm tra quyền, trạng thái phát hành và checksum; lỗi 403/404/409 được hiển thị, không dùng bản cũ để thay thế. Bản đã chia sẻ nằm trong cache thiết bị cho ứng dụng nhận đọc; chưa có xuất PDF/in native.

- Login native gửi header `x-luxcare-client: native`. Backend phải trả refresh token cho native; refresh nhận token trong body, web tiếp tục dùng cookie HttpOnly.
- Mobile dùng Bearer access token trong bộ nhớ và refresh token trong SecureStore. Giữ chính sách phiên của backend; đăng nhập có thể thay thế phiên web.
- Các route phải giữ module/permission guard và phạm vi công ty/chi nhánh. Mobile gửi `x-branch-id` khi chọn chi nhánh; một số API còn nhận branchId trong query.
- Tuyển dụng cần bản sửa xóa/khôi phục ứng viên trong `server/service/recruitment-applicant.service.ts` của LuxCare: xóa truy vấn bản ghi chưa xóa, khôi phục truy vấn bản ghi đã xóa và cập nhật metadata tương ứng.
- Phòng ban cần xử lý tên cũ bằng mẫu literal đã escape trong `server/service/department.service.ts` và `department-legacy-pattern.ts` của LuxCare.
- Tải tài liệu dùng media proxy có xác thực và danh sách domain cho phép. Upload tài liệu/đơn từ cần các route managed upload tương ứng; tệp công khai tuyển dụng dùng `/api/v1/recruitment/files/public`.
- Hợp đồng chưa có version token chống cập nhật đồng thời. Mobile chỉ PATCH trường thay đổi; cần đối soát nếu nhiều người cùng sửa.
- Gia hạn trả `{ contract, extension }`, cập nhật hạn và chuyển expired về active. Backend hiện tạo lịch sử trước rồi lưu hợp đồng, chưa có transaction/idempotency chung; sau lỗi không rõ kết quả cần kiểm tra cả hai bản ghi trước khi gửi lại.
- Khung giờ check-in/out của ca hiện được lưu cấu hình nhưng backend chưa thực thi đầy đủ. Cờ xác nhận khuôn mặt lấy từ nguồn cấu hình chung, không tự bật ở mobile.

Các sửa đổi backend không được tự triển khai khi push repository mobile. Đối chiếu [ma trận triển khai](../mobile/IMPLEMENTATION.md) và kiểm thử trên staging trước nghiệm thu.

Phiếu lương cá nhân gọi `/api/v1/payroll/employee/me/payslips` và `/api/v1/payroll/runs/:id/lines/:employeeId`. Backend yêu cầu công ty/chi nhánh xác thực, chỉ trả phiếu đã phát hành của chính nhân viên với kỳ đã chốt/trả và checksum phát hành còn khớp. API chi tiết kiểm tra quyền đọc lương hoặc quyền sở hữu cùng bản phát hành; mobile không yêu cầu quyền quản lý cho luồng cá nhân. Danh sách hiện không có phân trang. Nguồn FE bổ sung ở đợt 39: `src/services/payrollService.ts` (tách transport) và `src/components/hr/payrollDetails.ts` (helper hiển thị), ngoài snapshot tách repository ban đầu.

Tài liệu chứng chỉ dùng `POST /api/v1/hr-credentials/upload?companyCode=...`, nhận data URL và trả `{ url, uploadToken }`. Khi tạo/sửa, mobile gửi `fileUrl`, `fileName`, `fileMimeType`, `fileSize`, `uploadToken`; không tự gán `resourceId`. Ứng dụng hỗ trợ PDF/JPEG/PNG/WebP/DOC/DOCX/XLS/XLSX tối đa 10 MB. Cần triển khai bản backend bổ sung MIME Word/Excel tại server/router/hr-credential.router.ts trước khi sử dụng các định dạng Office. Token được hoàn tất vào nguồn `hr.credential` khi lưu hồ sơ. Bỏ chọn không xóa pending trên server; thay tài liệu không đảm bảo dọn tài nguyên cũ. Backend chưa có version/transaction cho cập nhật này; nếu kết quả lưu chưa xác định, cần tải lại trước khi gửi tiếp.

Upload hợp đồng mới/gia hạn hiện gửi các trường đơn (`contractFileUploadToken`, `signedImageUploadToken`, `extensionFileUploadToken`, `extensionSignedImageUploadToken`). Schema route hiện chưa nhận các mảng nhiều tệp dù controller/model có hỗ trợ mảng. Cần đồng bộ schema và hành vi cập nhật trước khi mở thay/gỡ tệp đã lưu trên mobile. Token pending hết hạn sau 24 giờ; bỏ chọn trên mobile không xóa tệp server.
